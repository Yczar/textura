name: textura
homepage: https://github.com/Yczar/textura
description: Your friendly Flutter companion that wraps your widgets in a cozy blanket of beautiful textures.
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ">=5.1.0 <7.0.0"
